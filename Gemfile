# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.1.2'

# Specify your gem's dependencies in sync.gemspec.
gemspec

gem 'sqlite3'

group :development do
  gem 'awesome_print'
  gem 'bullet'
  gem 'pry'
  gem 'rubocop-rails'
  gem 'rubocop-shopify'
end

gem 'sprockets-rails'

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
